tools:
  foobar:
    title: Foo Bar
    description: A dummy tool to exemplify the YAML file
    command: "node run.js"
    parameters:
      foo_int:
        type: integer
      foo_float:
        type: float
      foo_string:
        type: string
      foo_enum:
        type: enum
        values:
          - foo
          - bar
          - baz
      foo_array:
        type: integer
        array: true
    data:
      foo_matrix:
        extension: dat
        description: A matrix file
      foo_csv:
        extension: csv
        description: A CSV file